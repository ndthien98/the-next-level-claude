# SKILLS — useful for this project

> List which Claude Code skills are relevant for this project's domain.
> Specialists spawned by the lead will read this to know what's available.

Project domain: (fill in — e.g. "Node.js backend with PostgreSQL")

Recommended skills (subset of installed ones):
- `code-review` — before any PR
- `debug` — when an error appears
- `plan` — at the start of a new feature
- (add per-domain skills, e.g. `nestjs-expert`, `react-expert`,
  `blockchain-engineer`, `postgres-pro`)

Built-in:
- `token-efficiency` — always
- `karpathy-guidelines` — write/review/refactor
