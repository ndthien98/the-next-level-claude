# TOOLS — allowed Bash commands for this project

> The lead may pass `--allowedTools` style hints to specialists when
> spawning them. List commands the project actually uses; specialists
> can use them without prompting.

Examples:
- `Bash(yarn *)`
- `Bash(npm *)`
- `Bash(pnpm *)`
- `Bash(git *)`
- `Bash(docker *)`
- `Bash(psql *)`
- `Bash(pytest *)`
- `Bash(forge *)` (Foundry)
- `Bash(npx hardhat *)`

Add what your stack actually needs. Anything not listed: ask before
running.
